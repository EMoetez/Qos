nifi-spark-pipeline/
│
├── Dockerfile                  # Full Dockerfile that installs NiFi, Spark, ES, Kibana, etc.
├── docker-compose.yml         # Optional for local multi-container setup (good for dev)
├── entrypoint.py              # Python script to start all processors/services
├── spark_service/
│   └── spark_service.py       # Flask API to process data chunks from NiFi
├── flow/
│   └── flow.json.gz           # Your NiFi flow template
├── requirements.txt           # For Python dependencies
├── scripts/
│   └── setup_env.sh           # Optional: Script to create data folders etc.
├── data/                      # Optional: Sample input data
│   ├── landing/
│   ├── staging/
│   ├── processed/
│   └── archive/
├── .github/
│   └── workflows/
│       └── docker.yml         # GitHub Actions workflow to build & push Docker image
├── README.md                  # How to build, run, and use the image
└── .env                       # (optional) Environment variables (ports, passwords...)
